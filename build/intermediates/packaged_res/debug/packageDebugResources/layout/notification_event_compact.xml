<?xml version="1.0" encoding="utf-8"?>
<!--
    notification_event_compact.xml
    ───────────────────────────────────────────────────────────────────────────
    Compact (collapsed) notification view for Resolve event reminders.

    Design rules:
    • LinearLayout only — no RelativeLayout (RemoteViews constraint parity)
    • No hardcoded colour backgrounds — transparent lets the system shade apply its colour
    • maxLines + ellipsize on every dynamic text field to guard against overflow
    • Accessibility: contentDescription on icon; importantForAccessibility on static labels
    • 8 dp spacing grid throughout
    • Render-safe in RemoteViews and in the Notification Tailoring Studio in-app preview

    IDs are intentionally shared with notification_event_expanded.xml so that
    NotificationLayoutFactory and NotificationStyleApplier can operate on either
    layout with the same binding code.
    ───────────────────────────────────────────────────────────────────────────
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/notification_compact_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <!-- ── Row 1 : header (icon · label · spacer · time) ── -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="6dp">

        <ImageView
            android:id="@+id/notification_icon"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:src="@drawable/ic_system_down_triangle"
            android:tint="@color/system_cyan"
            android:importantForAccessibility="no"
            android:contentDescription="@null" />

        <TextView
            android:id="@+id/notification_header_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:text="SYSTEM"
            android:textColor="@color/system_cyan"
            android:textSize="10sp"
            android:textStyle="bold"
            android:letterSpacing="0.1"
            android:maxLines="1"
            android:ellipsize="end"
            android:importantForAccessibility="no" />

        <!-- Flexible spacer pushes time text to trailing edge -->
        <android.widget.Space
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <TextView
            android:id="@+id/notification_time_header"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="10:00 AM"
            android:textColor="@color/dim_white"
            android:textSize="10sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:importantForAccessibility="no" />

    </LinearLayout>

    <!-- ── Row 2 : event title ── -->
    <TextView
        android:id="@+id/notification_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="TEAM MEETING"
        android:textColor="@android:color/white"
        android:textSize="15sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_marginBottom="2dp"
        android:contentDescription="Event title" />

    <!-- ── Row 3 : countdown / time body ── -->
    <TextView
        android:id="@+id/notification_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="IN 15 MIN"
        android:textColor="@color/system_cyan"
        android:textSize="13sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end"
        android:contentDescription="Time until event" />

</LinearLayout>
